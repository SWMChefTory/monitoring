apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://${PROMETHEUS_HOST}:${PROMETHEUS_PORT}
    uid: prometheusdatasource
    isDefault: true
    jsonData:
      timeInterval: 15s